{
  "name": "@vendoora/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "type-check": "tsc --noEmit",
    "lint": "eslint .",
    "test": "echo \"worker jobs are exercised via @vendoora/domain integration tests\" && exit 0",
    "build": "echo \"no build step (run via tsx)\" && exit 0"
  },
  "dependencies": {
    "@vendoora/db": "workspace:*",
    "@vendoora/domain": "workspace:*",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@vendoora/config": "workspace:*",
    "@types/node": "^22.9.0",
    "eslint": "^9.15.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
