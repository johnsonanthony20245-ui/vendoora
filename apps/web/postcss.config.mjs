// PostCSS config for Tailwind v4. The @tailwindcss/postcss plugin
// reads the @theme directive in app/globals.css and emits utility
// classes plus exposed CSS variables.
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
